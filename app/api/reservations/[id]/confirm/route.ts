import { confirmReservation } from "../../../../../lib/data"; import { adminUser } from "../../../../../lib/api-auth";
export async function POST(_request:Request,{params}:{params:Promise<{id:string}>}){const user=await adminUser();if(user instanceof Response)return user;try{return Response.json(await confirmReservation((await params).id));}catch(error){return Response.json({error:error instanceof Error?error.message:"No pudimos confirmar la reserva."},{status:400});}}
