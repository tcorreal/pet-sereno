import type { Metadata } from "next"; import { headers } from "next/headers"; import "./globals.css";
export async function generateMetadata():Promise<Metadata>{const h=await headers();const host=h.get("x-forwarded-host")??h.get("host")??"localhost:3000";const protocol=h.get("x-forwarded-proto")??(host.includes("localhost")?"http":"https");const origin=`${protocol}://${host}`;return {metadataBase:new URL(origin),title:{default:"Pet Sereno — Club de Mascotas",template:"%s · Pet Sereno"},description:"Libertad para ellos. Tranquilidad para ti. Cuidado, naturaleza y bienestar para tu mascota en Medellín.",openGraph:{title:"Pet Sereno — Club de Mascotas",description:"Libertad para ellos. Tranquilidad para ti.",locale:"es_CO",type:"website",images:[{url:`${origin}/og.png`,width:1792,height:896,alt:"Pet Sereno — Libertad para ellos. Tranquilidad para ti."}]},twitter:{card:"summary_large_image",title:"Pet Sereno — Club de Mascotas",description:"Libertad para ellos. Tranquilidad para ti.",images:[`${origin}/og.png`]}}}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="es"><body>{children}</body></html>}
