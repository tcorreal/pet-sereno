import { AppLink as Link } from "./app-link"; import { Logo,StatusBadge } from "./site-shell";
export function AdminShell({children,user}:{children:React.ReactNode;user:string}){const links=[["/admin","Resumen"],["/admin/clientes","Clientes"],["/admin/mascotas","Mascotas"],["/admin/reservas","Reservas"],["/admin/servicios","Servicios"],["/admin/configuracion","Configuración"]];return <div className="admin-shell"><aside className="admin-sidebar"><Logo inverse/><span className="admin-label">OPERACIÓN</span><nav>{links.map(([href,label])=><Link href={href} key={href}>{label}</Link>)}</nav><div className="admin-user"><span>{user.slice(0,1).toUpperCase()}</span><div><strong>{user}</strong><small>Administración</small></div></div></aside><main className="admin-main">{children}</main></div>}
export function AdminHeader({eyebrow,title,copy,action}:{eyebrow?:string;title:string;copy?:string;action?:React.ReactNode}){return <header className="admin-header"><div>{eyebrow&&<span className="eyebrow">{eyebrow}</span>}<h1>{title}</h1>{copy&&<p>{copy}</p>}</div>{action}</header>}
export function DataTable({columns,children}:{columns:string[];children:React.ReactNode}){return <div className="table-wrap"><table><thead><tr>{columns.map((c)=><th key={c}>{c}</th>)}</tr></thead><tbody>{children}</tbody></table></div>}
export {StatusBadge};
