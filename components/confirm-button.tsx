"use client"; import { useState } from "react"; import { fetchJson } from "../lib/client-fetch"; import { refreshPage } from "../lib/client-navigation";
export function ConfirmButton({id}:{id:string}){const [busy,setBusy]=useState(false);const [error,setError]=useState("");return <div><button type="button" className="button button--primary button--small" disabled={busy} onClick={async()=>{setBusy(true);setError("");try{await fetchJson(`/api/reservations/${id}/confirm`,{method:"POST"},"No pudimos confirmar la reserva.");refreshPage();}catch(e){setError(e instanceof Error?e.message:"No pudimos confirmar la reserva.");}finally{setBusy(false)}}}>{busy?"Confirmando…":"Confirmar"}</button>{error&&<small className="inline-error">{error}</small>}</div>}
